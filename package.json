{
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "npm run --workspace=builder-ui dev",
		"build": "npm run --workspace=builder-ui build",
		"lint": "npm run --workspace=builder-ui lint"
	}
}
